var documenterSearchIndex = {"docs":
[{"category":"section","location":"tutorials/01_ground_state/#1.-The-composite-fermion-ground-state","page":"1 — Ground state & density","text":"This first tutorial builds the nu = 13 composite-fermion ground state, samples it with the Metropolis–Hastings–Gibbs walk, and measures its density profile on the sphere. By the end you will know the whole workflow: build → thermalize → sample → measure.\n\nWe keep the system small and the chains short so the page builds quickly; for production runs you would use more particles and far more steps.\n\nusing CFsOnSphere\nusing Random\nusing LinearAlgebra\nLinearAlgebra.BLAS.set_num_threads(1)   # small dense determinants: single-threaded BLAS wins\nusing Plots\ngr()","title":"1. The composite-fermion ground state"},{"category":"section","location":"tutorials/01_ground_state/#Building-the-state","page":"1 — Ground state & density","text":"A Jain state is fixed by three integers: the number of particles N, the number of filled Lambda-levels n, and the Jastrow power p (the number of attached vortices, even). They give the filling nu = n(pn+1). The helper cf_ground_state_lm returns the effective monopole strength Qstar and the occupied (L L_z) orbitals.\n\nHere p = 2 (one vortex pair) and n = 1, i.e. the nu = 13 Laughlin state.\n\nN, n, p = 10, 1, 2           # ν = n/(pn+1) = 1/3\nQstar, l_m_list = cf_ground_state_lm(N, n, p)\n@show Qstar length(l_m_list);\nnothing #hide\n\nWe allocate two wavefunction buffers — the current (accepted) state and the proposed state. Ψproj is the Jain–Kamilla projected composite-fermion wavefunction.\n\nψ      = Ψproj(Qstar, p, N, l_m_list)\nψ_next = Ψproj(Qstar, p, N, l_m_list)","title":"Building the state"},{"category":"section","location":"tutorials/01_ground_state/#The-sampling-weight","page":"1 — Ground state & density","text":"We sample positions with probability Psi^2. The walker maximises a log probability, and Psi^2 = det^2textJastrow^2, so:\n\nlogpdf(ψ) = 2.0 * real(logdet(ψ.slater_det) + ψ.jastrow_factor_log)","title":"The sampling weight"},{"category":"section","location":"tutorials/01_ground_state/#Thermalizing","page":"1 — Ground state & density","text":"Start from random positions and let gibbs_thermalization! equilibrate the chain while auto-tuning the proposal step size σ toward a 50% acceptance rate. It returns the index of the next particle to move, the tuned σ, a timing, and the acceptance rate.\n\nrng = MersenneTwister(2024)\nθ, ϕ = rand_θ_ϕ_gen(rng, N)\nθ_next, ϕ_next = copy(θ), copy(ϕ)\nσ = π / sqrt(12)\n\nsampling_iter, σ, _, therm_accept =\n    gibbs_thermalization!(rng, ψ, ψ_next, θ, ϕ, θ_next, ϕ_next, σ, logpdf, 100_000)\n@show therm_accept σ;\nnothing #hide","title":"Thermalizing"},{"category":"section","location":"tutorials/01_ground_state/#Measuring-the-density","page":"1 — Ground state & density","text":"We accumulate a histogram of the polar angle theta in equal-area bins (uniform in costheta). update_density! adds the current configuration to the histogram. We wrap the Metropolis loop in a function so all loop variables stay in local scope.\n\nfunction sample_density(rng, ψ, ψ_next, θ, ϕ, θ_next, ϕ_next, σ, sampling_iter, logpdf;\n                        n_steps = 1_000_000, n_bins = 30)\n    N = length(θ)\n    θmesh = acos.(LinRange(1.0, -1.0, n_bins + 1))        # equal-area bin edges\n    Agrid = 2π .* (cos.(θmesh[1:end-1]) .- cos.(θmesh[2:end]))\n    density = zeros(Float64, n_bins)\n\n    logpdf_current = logpdf(ψ)\n    accepted = 0\n    for _ in 1:n_steps\n        i = sampling_iter\n        θ_next[i], ϕ_next[i] = proposal(rng, θ[i], ϕ[i], σ)\n        update_wavefunction!(ψ_next, θ_next[i], ϕ_next[i], i)\n        logpdf_next = logpdf(ψ_next)\n\n        if logpdf_next - logpdf_current >= log(rand(rng))\n            θ[i], ϕ[i] = θ_next[i], ϕ_next[i]\n            copy!(ψ, ψ_next, i)\n            logpdf_current = logpdf_next\n            accepted += 1\n        else\n            θ_next[i], ϕ_next[i] = θ[i], ϕ[i]\n            copy!(ψ_next, ψ, i)\n        end\n\n        update_density!(θmesh, θ, density)\n        sampling_iter = mod(sampling_iter, N) + 1\n    end\n\n    θcenters = 0.5 .* (θmesh[1:end-1] .+ θmesh[2:end])\n    return θcenters, density ./ n_steps ./ Agrid, accepted / n_steps\nend\n\nθc, density, accept = sample_density(rng, ψ, ψ_next, θ, ϕ, θ_next, ϕ_next, σ, sampling_iter, logpdf)\n@show accept;\nnothing #hide\n\nWe report the density in magnetic units — the local filling nu(theta), whose bulk value is the filling fraction itself. With the monopole strength Q_textshift = N2nu, nu(theta) = 2pin(theta)Q_textshift. On the edgeless sphere the bulk is a flat plateau at nu; plotting from 0 keeps it in context rather than zooming into the noise.\n\nν      = n / (p*n + 1)                 # = 1/3\nQshift = N / (2ν)\nνθ     = density .* (2π / Qshift)       # local filling, flat value = ν\n\nplot(θc, νθ; lw=2, marker=:circle, ms=3, label=\"measured\", ylims=(0.0, 0.5),\n     xlabel=\"θ\", ylabel=\"local filling  ν(θ)\", title=\"ν = 1/3 ground state (N = $N)\")\nhline!([ν]; ls=:dash, label=\"ν = 1/3\")\n\n\n\nThis page was generated using Literate.jl.","title":"Measuring the density"},{"category":"section","location":"architecture/#Architecture","page":"Architecture","text":"How the package is organized, for contributors and anyone who wants to understand or extend the internals.","title":"Architecture"},{"category":"section","location":"architecture/#Source-layout","page":"Architecture","text":"CFsOnSphere is a single flat module (src/CFsOnSphere.jl) that includes small, single-responsibility files in dependency order:\n\nFile Contents\ncalculate_j_y_eigenstates.jl J_y eigenstate table (disk-memoised in tempdir())\nspinor_coordinates.jl u_v_generator(θ, ϕ)\nmonopole_harmonics.jl calculate_ll — single-particle harmonics\nsymmetric_polynomials.jl get_symmetric_polynomials! (elementary symmetric polynomials)\njk_projection_utilities.jl projection_coeff, generate_fourier_matrices\nlegendre_polynomials.jl legendre_polynomials!\nprojected_wavefunction.jl Ψproj, Ψparton: types, constructors, updates, copy!\nunprojected_wavefunction.jl Ψunproj, ΨoneLL: types, constructors, updates, copy!\nslater_inverse.jl Sherman–Morrison + build_extended_slater! (Ψunproj)\nlambda_levels.jl cf_ground_state_lm / cf_quasihole_lm / cf_quasiparticle_lm\nmonte_carlo.jl proposal, thermalization, density accumulators\n\nExternal dependencies are deliberately light: LinearAlgebra, Random, JLD2, Combinatorics, Serialization, SpecialFunctions. BLAS is pinned to one thread (multi-thread BLAS hurts performance for the small dense determinants used here).","title":"Source layout"},{"category":"section","location":"architecture/#Projected-vs.-unprojected:-the-key-structural-split","page":"Architecture","text":"The two families differ in how a single particle move propagates:\n\nΨunproj uses single-particle orbitals Y_Q^starlm(Omega_i). Moving particle i changes only one column of the Slater matrix, so the determinant ratio and inverse can be tracked by a rank-1 Sherman–Morrison update (slater_inverse.jl): O(N) for the acceptance ratio, O(N²) for the inverse refresh. This is the fast path.\nΨproj / Ψparton use multi-particle projected orbitals: the elementary symmetric polynomials couple all particles, so moving one particle changes every column. There is no rank-1 shortcut; the determinant is recomputed via logdet. This is why the projected updates rebuild the symmetric polynomials and the whole Slater matrix.","title":"Projected vs. unprojected: the key structural split"},{"category":"section","location":"architecture/#The-shared-update-path","page":"Architecture","text":"Ψproj and Ψparton share their update machinery (_update_pairs! and the update_wavefunction! methods in projected_wavefunction.jl):\n\nUpdate spinors, pairwise Jastrow log, vortex ratios, and chord distances.\nRebuild the elementary symmetric polynomials per particle (get_symmetric_polynomials!).\nContract the Fourier/Wigner-D matrices with the polynomials to form each Slater column.\n\nThe update routines are written to be allocation-free on the hot path (in-place mul!, broadcast into preallocated buffers, in-place spinor arrays), so the projected sampler runs at the same allocation profile as a hand-written loop.","title":"The shared update path"},{"category":"section","location":"architecture/#Adding-a-new-wavefunction-type","page":"Architecture","text":"Mirror the existing pattern: define the mutable struct and constructor, implement update_wavefunction! (full and single-particle-move variants) and copy! (full and partial), and — if the orbitals are single-particle — the Sherman–Morrison interface. Add the type to the Union in gibbs_thermalization! so the generic driver accepts it.","title":"Adding a new wavefunction type"},{"category":"section","location":"architecture/#C-port","page":"Architecture","text":"A native, header-only C++ core (Eigen + CMake) mirrors the projected and unprojected samplers under cpp/. It is validated against the Julia reference to le 10^-12; see Validation.","title":"C++ port"},{"category":"section","location":"tutorials/06_observables/#6.-Observables:-pair-correlation-and-energy","page":"6 — Observables & energies","text":"Beyond the density, the two workhorses of FQHE Monte Carlo are the pair-correlation function g(r) and the interaction energy per particle. Both are accumulated from the same Metropolis chain. We use the chord distance r_ij = 2u_iv_j - u_jv_i between particles on the unit sphere, which the wavefunction already caches in ψ.dist_matrix.\n\nusing CFsOnSphere\nusing Random\nusing LinearAlgebra\nusing Statistics\nLinearAlgebra.BLAS.set_num_threads(1)\nusing Plots\ngr()\n\nThe chain below records, at each step, (i) a histogram of pair distances for g(r) and (ii) the Coulomb energy of the configuration, V = sum_ij 1r_ij, for the energy estimate.\n\nfunction sample_observables(N, n, p; seed = 3, n_therm = 5_000, n_steps = 40_000, n_bins = 40)\n    Qstar, l_m_list = cf_ground_state_lm(N, n, p)\n    rng = MersenneTwister(seed)\n    ψ, ψ_next = Ψproj(Qstar, p, N, l_m_list), Ψproj(Qstar, p, N, l_m_list)\n    logpdf(ψ) = 2.0 * real(logdet(ψ.slater_det) + ψ.jastrow_factor_log)\n\n    θ, ϕ = rand_θ_ϕ_gen(rng, N)\n    θ_next, ϕ_next = copy(θ), copy(ϕ)\n    σ = π / sqrt(12)\n    it, σ, _, _ = gibbs_thermalization!(rng, ψ, ψ_next, θ, ϕ, θ_next, ϕ_next, σ, logpdf, n_therm)\n\n    redges = LinRange(0.0, 2.0, n_bins + 1)          # chord distance ∈ [0, 2]\n    dr = redges[2] - redges[1]\n    gcount = zeros(Float64, n_bins)\n    energy_samples = Float64[]\n\n    coulomb(ψ) = sum(1.0 / ψ.dist_matrix[k, i] for i in 1:N-1 for k in i:N-1)\n\n    lpc = logpdf(ψ)\n    for _ in 1:n_steps\n        θ_next[it], ϕ_next[it] = proposal(rng, θ[it], ϕ[it], σ)\n        update_wavefunction!(ψ_next, θ_next[it], ϕ_next[it], it)\n        lpn = logpdf(ψ_next)\n        if lpn - lpc >= log(rand(rng))\n            θ[it], ϕ[it] = θ_next[it], ϕ_next[it]; copy!(ψ, ψ_next, it); lpc = lpn\n        else\n            θ_next[it], ϕ_next[it] = θ[it], ϕ[it]; copy!(ψ_next, ψ, it)\n        end\n        # accumulate observables from the current accepted state\n        for i in 1:N-1, k in i:N-1\n            b = min(n_bins, 1 + floor(Int, ψ.dist_matrix[k, i] / dr))\n            gcount[b] += 1.0\n        end\n        push!(energy_samples, coulomb(ψ))\n        it = mod(it, N) + 1\n    end\n\n    rc = 0.5 .* (redges[1:end-1] .+ redges[2:end])\n    return rc, gcount ./ n_steps, energy_samples\nend\n\nWe measure the nu = 13 ground state (n = 1, p = 2).\n\nrc, g, E = sample_observables(7, 1, 2);\nnothing #hide","title":"6. Observables: pair correlation and energy"},{"category":"section","location":"tutorials/06_observables/#Energy-with-a-Monte-Carlo-error-bar","page":"6 — Observables & energies","text":"Samples in an MCMC chain are correlated, so a naive standard error underestimates the uncertainty. Block averaging — averaging within consecutive blocks and taking the standard error of the block means — gives an honest estimate.\n\nfunction block_error(x; n_blocks = 20)\n    bs = length(x) ÷ n_blocks\n    means = [mean(@view x[(b-1)*bs+1 : b*bs]) for b in 1:n_blocks]\n    return mean(means), std(means) / sqrt(n_blocks)\nend\n\nEmean, Eerr = block_error(E)\nprintln(\"Coulomb energy V = \", round(Emean, digits=4), \" ± \", round(Eerr, digits=4),\n        \"  (\", round(Emean / 7, digits=4), \" per particle)\")","title":"Energy with a Monte Carlo error bar"},{"category":"section","location":"tutorials/06_observables/#The-pair-correlation-histogram","page":"6 — Observables & energies","text":"The distribution of pair distances shows the FQHE correlation hole: the probability of two particles approaching vanishes as they coincide, the hallmark of an incompressible liquid.\n\nplot(rc, g; lw=2, marker=:circle, ms=3, legend=false,\n     xlabel=\"chord distance r\", ylabel=\"pair count  ∝ g(r)\",\n     title=\"ν = 1/3 pair correlation (N = 7)\")\n\n\n\nThis page was generated using Literate.jl.","title":"The pair-correlation histogram"},{"category":"section","location":"tutorials/02_excitations/#2.-Quasiholes-and-quasiparticles","page":"2 — Quasiholes & quasiparticles","text":"The elementary charged excitations of a Jain state are the quasihole (a local depletion of density) and the quasiparticle (a local excess). On the sphere we create one at the north pole by removing or adding a single orbital at the top of the occupied Lambda-levels. The builders cf_quasihole_lm and cf_quasiparticle_lm do exactly that.\n\nWe reuse the build → thermalize → sample density recipe from tutorial 1, so here we focus on what changes.\n\nusing CFsOnSphere\nusing Random\nusing LinearAlgebra\nLinearAlgebra.BLAS.set_num_threads(1)\nusing Plots\ngr()\n\nA small, self-contained density sampler (same algorithm as tutorial 1).\n\nfunction density_profile(Qstar, p, l_m_list; seed = 7, n_therm = 100_000, n_steps = 1_000_000, n_bins = 30)\n    N = length(l_m_list)\n    rng = MersenneTwister(seed)\n    ψ, ψ_next = Ψproj(Qstar, p, N, l_m_list), Ψproj(Qstar, p, N, l_m_list)\n    logpdf(ψ) = 2.0 * real(logdet(ψ.slater_det) + ψ.jastrow_factor_log)\n\n    θ, ϕ = rand_θ_ϕ_gen(rng, N)\n    θ_next, ϕ_next = copy(θ), copy(ϕ)\n    σ = π / sqrt(12)\n    sampling_iter, σ, _, _ = gibbs_thermalization!(rng, ψ, ψ_next, θ, ϕ, θ_next, ϕ_next, σ, logpdf, n_therm)\n\n    θmesh = acos.(LinRange(1.0, -1.0, n_bins + 1))\n    Agrid = 2π .* (cos.(θmesh[1:end-1]) .- cos.(θmesh[2:end]))\n    density = zeros(Float64, n_bins)\n    logpdf_current = logpdf(ψ)\n    for _ in 1:n_steps\n        i = sampling_iter\n        θ_next[i], ϕ_next[i] = proposal(rng, θ[i], ϕ[i], σ)\n        update_wavefunction!(ψ_next, θ_next[i], ϕ_next[i], i)\n        logpdf_next = logpdf(ψ_next)\n        if logpdf_next - logpdf_current >= log(rand(rng))\n            θ[i], ϕ[i] = θ_next[i], ϕ_next[i]; copy!(ψ, ψ_next, i); logpdf_current = logpdf_next\n        else\n            θ_next[i], ϕ_next[i] = θ[i], ϕ[i]; copy!(ψ_next, ψ, i)\n        end\n        update_density!(θmesh, θ, density)\n        sampling_iter = mod(sampling_iter, N) + 1\n    end\n    θc = 0.5 .* (θmesh[1:end-1] .+ θmesh[2:end])\n    return θc, density ./ n_steps ./ Agrid\nend","title":"2. Quasiholes and quasiparticles"},{"category":"section","location":"tutorials/02_excitations/#Three-states-at-ν-1/3","page":"2 — Quasiholes & quasiparticles","text":"We compare the ground state with one quasihole and one quasiparticle. Note that the builders return different particle numbers (a quasihole removes an orbital, a quasiparticle adds one), which is the correct sphere bookkeeping.\n\nn, p = 1, 2                  # ν = n/(pn+1) = 1/3\nQgs, lm_gs = cf_ground_state_lm(7, n, p)\nQqh, lm_qh = cf_quasihole_lm(7, n, p)\nQqp, lm_qp = cf_quasiparticle_lm(7, n, p)\n@show length(lm_gs) length(lm_qh) length(lm_qp);\n\nθc_gs, n_gs = density_profile(Qgs, p, lm_gs)\nθc_qh, n_qh = density_profile(Qqh, p, lm_qh)\nθc_qp, n_qp = density_profile(Qqp, p, lm_qp)\n\nWe plot the local filling nu(theta) = 2pin(theta)Q_textshift (each state with its own Q_textshift = N2nu), so the bulk sits at nu = 13. The excitations live at the north pole (theta = 0): the quasihole depletes the filling there, the quasiparticle enhances it, while the ground state is flat. Showing the y=0 baseline keeps the dip/bump in context.\n\nν = 1 / 3\nlocfill(dens, lm) = dens .* (2π / (length(lm) / (2ν)))   # local filling for a state with these orbitals\n\nplot(xlabel=\"θ\", ylabel=\"local filling  ν(θ)\", ylims=(0.0, 0.7),\n     title=\"ν = 1/3: ground state vs. excitations\", legend=:bottom)\nplot!(θc_gs, locfill(n_gs, lm_gs); lw=2, label=\"ground state\")\nplot!(θc_qh, locfill(n_qh, lm_qh); lw=2, label=\"quasihole\")\nplot!(θc_qp, locfill(n_qp, lm_qp); lw=2, label=\"quasiparticle\")\nhline!([ν]; ls=:dash, color=:black, label=\"ν = 1/3\")\n\n\n\nThis page was generated using Literate.jl.","title":"Three states at ν = 1/3"},{"category":"section","location":"tutorials/03_higher_fillings/#3.-Higher-fillings-via-the-outer-Jastrow","page":"3 — Higher fillings (outer Jastrow)","text":"The Jain sequence has two knobs: the number of filled Lambda-levels n, and the Jastrow power p (the number of attached vortices, even). The filling is nu = n(pn+1):\n\np n nu\n2 1 1/3\n2 2 2/5\n4 1 1/5\n4 2 2/9\n\nA key design point of this package: the Jain–Kamilla projection always binds a single vortex pair, and any higher filling is reached just by raising p — the extra vortices ride along as an outer holomorphic Jastrow factor that stays in the lowest Landau level automatically,\n\nPsi = Phi_1^p-2mathcalP_mathrmLLLPhi_nPhi_1^2qquad\nPhi_1=prod_jk(u_jv_k-u_kv_j)\n\nSo you never bind more than one pair inside the projection. Let us see both halves of this.\n\nusing CFsOnSphere\nusing Random\nusing LinearAlgebra\nLinearAlgebra.BLAS.set_num_threads(1)\nusing Plots\ngr()","title":"3. Higher fillings via the outer Jastrow"},{"category":"section","location":"tutorials/03_higher_fillings/#Single-Λ-level:-the-state-is-exactly-a-Laughlin-polynomial","page":"3 — Higher fillings (outer Jastrow)","text":"For n = 1 the projection is trivial and the identity above collapses to Psi = Phi_1^p+1 — the Laughlin state at nu = 1(p+1). We can check this directly: build Ψproj and compare it to Phi_1^p+1 at several random configurations. If they agree up to a single overall constant, the difference logΨ - (p+1) logΦ₁ is the same for every configuration.\n\nlogΦ1(U, V, N) = sum(log(U[i]*V[j] - V[i]*U[j]) for i in 1:N-1 for j in i+1:N)\n\nfunction laughlin_check(N, p; seed = 11)\n    Qstar, lm = cf_ground_state_lm(N, 1, p)\n    ψ = Ψproj(Qstar, p, N, lm)\n    rng = MersenneTwister(seed)\n    diffs = ComplexF64[]\n    for _ in 1:4\n        θ, ϕ = rand_θ_ϕ_gen(rng, N)\n        update_wavefunction!(ψ, θ, ϕ)\n        U = cos.(θ ./ 2) .* exp.(0.5im .* ϕ)\n        V = sin.(θ ./ 2) .* exp.(-0.5im .* ϕ)\n        logΨ = logdet(ψ.slater_det) + ψ.jastrow_factor_log\n        push!(diffs, logΨ - (p + 1) * logΦ1(U, V, N))\n    end\n    maximum(abs.(real.(diffs) .- real(diffs[1])))\nend\n\nfor p in (2, 4)\n    spread = laughlin_check(6, p)\n    ν = \"1/$(p+1)\"\n    println(\"p = $p  (ν = $ν):  max |logΨ - (p+1)logΦ₁ - const| = \", round(spread, sigdigits=3))\nend\n\nBoth spreads are at the level of floating-point round-off: Ψproj at n=1 is the Laughlin state Phi_1^p+1, reached purely through the Jastrow power — no extra projection work.","title":"Single Λ-level: the state is exactly a Laughlin polynomial"},{"category":"section","location":"tutorials/03_higher_fillings/#Two-Λ-levels:-a-genuinely-projected-state,-ν-2/9","page":"3 — Higher fillings (outer Jastrow)","text":"For n ge 2 the projection is nontrivial. Building nu = 29 is no harder — set n = 2, p = 4 (two pairs). We sample its density just as in the earlier tutorials.\n\nfunction density_profile(Qstar, p, l_m_list; seed = 5, n_therm = 100_000, n_steps = 500_000, n_bins = 30)\n    N = length(l_m_list)\n    rng = MersenneTwister(seed)\n    ψ, ψ_next = Ψproj(Qstar, p, N, l_m_list), Ψproj(Qstar, p, N, l_m_list)\n    logpdf(ψ) = 2.0 * real(logdet(ψ.slater_det) + ψ.jastrow_factor_log)\n    θ, ϕ = rand_θ_ϕ_gen(rng, N)\n    θ_next, ϕ_next = copy(θ), copy(ϕ)\n    σ = π / sqrt(12)\n    it, σ, _, _ = gibbs_thermalization!(rng, ψ, ψ_next, θ, ϕ, θ_next, ϕ_next, σ, logpdf, n_therm)\n    θmesh = acos.(LinRange(1.0, -1.0, n_bins + 1))\n    Agrid = 2π .* (cos.(θmesh[1:end-1]) .- cos.(θmesh[2:end]))\n    density = zeros(Float64, n_bins)\n    lpc = logpdf(ψ)\n    for _ in 1:n_steps\n        θ_next[it], ϕ_next[it] = proposal(rng, θ[it], ϕ[it], σ)\n        update_wavefunction!(ψ_next, θ_next[it], ϕ_next[it], it)\n        lpn = logpdf(ψ_next)\n        if lpn - lpc >= log(rand(rng))\n            θ[it], ϕ[it] = θ_next[it], ϕ_next[it]; copy!(ψ, ψ_next, it); lpc = lpn\n        else\n            θ_next[it], ϕ_next[it] = θ[it], ϕ[it]; copy!(ψ_next, ψ, it)\n        end\n        update_density!(θmesh, θ, density)\n        it = mod(it, N) + 1\n    end\n    0.5 .* (θmesh[1:end-1] .+ θmesh[2:end]), density ./ n_steps ./ Agrid\nend\n\nN = 8\nQstar, lm = cf_ground_state_lm(N, 2, 4)   # n = 2, p = 4  ⇒  ν = 2/9\n@show Qstar length(lm)\nθc, dens = density_profile(Qstar, 4, lm)\n\nIn magnetic units the bulk is a flat plateau at the filling nu = 29 (using Q_textshift = N2nu), with the y=0 baseline shown for context.\n\nν      = 2 / 9\nQshift = N / (2ν)\nνθ     = dens .* (2π / Qshift)\n\nplot(θc, νθ; lw=2, marker=:circle, ms=3, label=\"measured\", ylims=(0.0, 0.35),\n     xlabel=\"θ\", ylabel=\"local filling  ν(θ)\", title=\"ν = 2/9 ground state (N = $N)\")\nhline!([ν]; ls=:dash, label=\"ν = 2/9\")\n\n\n\nThis page was generated using Literate.jl.","title":"Two Λ-levels: a genuinely projected state, ν = 2/9"},{"category":"section","location":"api/#API-reference","page":"API reference","text":"Everything exported by CFsOnSphere, grouped by topic. Internal helpers that carry docstrings are included for completeness.","title":"API reference"},{"category":"section","location":"api/#Wavefunction-types-and-updates","page":"API reference","text":"The four wavefunction families and their in-place update routines.","title":"Wavefunction types and updates"},{"category":"section","location":"api/#Orbitals,-projection,-and-polynomials","page":"API reference","text":"The single-particle monopole harmonics, the Jain–Kamilla projection coefficients, and the elementary symmetric polynomials at the heart of the projection.","title":"Orbitals, projection, and polynomials"},{"category":"section","location":"api/#Slater-inverse-and-excitations","page":"API reference","text":"Sherman–Morrison rank-1 inverse tracking for Ψunproj, and the extended Slater determinant for quasihole/quasiparticle amplitudes.","title":"Slater inverse and excitations"},{"category":"section","location":"api/#Λ-level-builders","page":"API reference","text":"Convenience constructors for the (Qstar, l_m_list) of the standard composite-fermion states.","title":"Λ-level builders"},{"category":"section","location":"api/#Monte-Carlo","page":"API reference","text":"The Metropolis–Hastings–Gibbs driver, proposal, step-size adaptation, and observable accumulators.","title":"Monte Carlo"},{"category":"module","location":"api/#CFsOnSphere","page":"API reference","text":"CFsOnSphere\n\nComposite-fermion wavefunctions on the Haldane sphere for the fractional quantum Hall effect.\n\nThe package builds Jain–Kamilla projected (Ψproj, Ψparton) and unprojected (Ψunproj, ΨoneLL) composite-fermion wavefunctions and samples them with a Metropolis–Hastings–Gibbs random walk to estimate densities, pair correlations, energies, and overlaps. The projection uses the quaternion/rotation reformulation of Jain–Kamilla projection (Phys. Rev. Lett. 134, 156501 (2025)): the projected orbital is a sum over a Wigner-D matrix times elementary symmetric polynomials of the Jastrow \"vortex ratios\".\n\nSee the documentation for tutorials and the full API reference.\n\n\n\n\n\n","title":"CFsOnSphere"},{"category":"type","location":"api/#CFsOnSphere.ΨoneLL","page":"API reference","text":"ΨoneLL\n\nBare single-Landau-level Jastrow wavefunction ∏_{j<k}(uⱼvₖ-uₖvⱼ)^p (no Slater determinant). Fields: p, system_size, U, V, dist_matrix, jastrow_factor_log.\n\n\n\n\n\n","title":"CFsOnSphere.ΨoneLL"},{"category":"method","location":"api/#CFsOnSphere.ΨoneLL-Tuple{Int64, Int64}","page":"API reference","text":"ΨoneLL(p, system_size)\n\nConstruct a bare Jastrow (Laughlin) wavefunction.\n\n\n\n\n\n","title":"CFsOnSphere.ΨoneLL"},{"category":"type","location":"api/#CFsOnSphere.Ψparton","page":"API reference","text":"Ψparton\n\nMutable state of a Jain-Kamilla projected parton wavefunction (multiple sectors with per-sector Qstars and l_m_lists). As in Ψproj, the JK projection binds a single vortex pair into each orbital (Q₁ = (N-1)/2).\n\nFields mirror Ψproj, with Qstars/l_m_lists replacing the single-sector versions, max_JK the largest JK order over sectors, and trackers[i] the slater_det row range of parton i.\n\n\n\n\n\n","title":"CFsOnSphere.Ψparton"},{"category":"method","location":"api/#CFsOnSphere.Ψparton-Tuple{Vector{Rational{Int64}}, Int64, Int64, Vector{Vector{Tuple{Rational{Int64}, Rational{Int64}}}}}","page":"API reference","text":"Ψparton(Qstars, p, system_size, l_m_lists)\n\nConstruct a Jain-Kamilla projected parton wavefunction. Qstars[i] and l_m_lists[i] give the effective monopole strength and occupied orbitals of parton i.\n\n\n\n\n\n","title":"CFsOnSphere.Ψparton"},{"category":"type","location":"api/#CFsOnSphere.Ψproj","page":"API reference","text":"Ψproj\n\nMutable state of a Jain-Kamilla projected composite-fermion wavefunction (electrons + p attached flux), at filling controlled by Qstar, l_m_list.\n\nThe JK projection binds a single vortex pair into each orbital (Q₁ = (N-1)/2, ESP roots with multiplicity one). Higher Jastrow powers ν = n/(2pn+1) come from the global Jastrow factor ∏(uᵢvⱼ-uⱼvᵢ)^p: e.g. Ψ_{n/(2pn+1)} = Φ₁^{2(p̃-1)} P_LLL[Φₙ Φ₁²] is obtained with p = 2 p̃.\n\nKey fields\n\nQstar::Rational{Int64}: effective monopole strength Q*.\np::Int64: power of the global Jastrow factor ∏(uᵢvⱼ-uⱼvᵢ)^p.\nsystem_size::Int64: number of electrons N.\nl_m_list: occupied (L, Lz) orbitals.\nslater_det::Matrix{ComplexF64}: the projected Slater matrix.\njastrow_factor_log::ComplexF64: log of the global Jastrow factor.\nreg_coeffs::Vector{Float64}: ESP normalization, reg_coeffs[i] = i/((N-1)-i+1) so ∏ = 1/C(N-1, k).\n\nThe remaining fields (U, V, exp_θ, exp_ϕ, dist_matrix, u_v_ratio_matrix, elementary_symmetric_polynomials, wigner_d_matrices, wigner_D_matrices, fourier_tot_matrix, μ_list, Lz_list, Lmax) are work arrays / caches.\n\n\n\n\n\n","title":"CFsOnSphere.Ψproj"},{"category":"method","location":"api/#CFsOnSphere.Ψproj-Tuple{Rational{Int64}, Int64, Int64, Vector{Tuple{Rational{Int64}, Rational{Int64}}}}","page":"API reference","text":"Ψproj(Qstar, p, system_size, l_m_list)\n\nConstruct a Jain-Kamilla projected CF wavefunction. Qstar = Q - p(N-1)/2 is the effective monopole strength, p the global Jastrow power, l_m_list the occupied (L, Lz) orbitals. The JK projection binds a single vortex pair into each orbital (Q₁ = (N-1)/2).\n\n\n\n\n\n","title":"CFsOnSphere.Ψproj"},{"category":"type","location":"api/#CFsOnSphere.Ψunproj","page":"API reference","text":"Ψunproj\n\nMutable state of an unprojected composite-fermion wavefunction with single-particle monopole-harmonic orbitals.\n\nKey fields\n\nQstar::Rational{Int64}: monopole strength of the orbitals.\np::Int64: power of the global Jastrow factor ∏(uᵢvⱼ-uⱼvᵢ)^p.\nsystem_size::Int64: number of electrons N.\nl_m_list: occupied (L, Lz) orbitals.\nU, V: spinor coordinates.\ndist_matrix::Matrix{Float64}: pairwise chord distances.\nslater_det::Matrix{ComplexF64}: slater_det[orbital, particle] = Y_{Q*,L,Lz}(Ωᵢ).\nslater_det_inv::Matrix{ComplexF64}: maintained inverse of slater_det (valid only for a square / closed-shell determinant; updated by the Sherman-Morrison helpers).\njastrow_factor_log::ComplexF64: log of the global Jastrow factor.\n\nfourier_tot_matrix, μ_list, Lz_list, exp_θ, exp_ϕ are caches for the orbitals.\n\n\n\n\n\n","title":"CFsOnSphere.Ψunproj"},{"category":"method","location":"api/#CFsOnSphere.Ψunproj-Tuple{Rational{Int64}, Int64, Int64, Vector{Tuple{Rational{Int64}, Rational{Int64}}}}","page":"API reference","text":"Ψunproj(Qstar, p, system_size, l_m_list)\n\nConstruct an unprojected CF wavefunction: a Slater determinant of monopole harmonics Y_{Qstar,L,Lz} (the orbitals in l_m_list) times the global Jastrow ∏(uᵢvⱼ-uⱼvᵢ)^p. The orbital transform is precomputed from the J_y eigenstates.\n\n\n\n\n\n","title":"CFsOnSphere.Ψunproj"},{"category":"method","location":"api/#Base.copy!-Tuple{Ψunproj, Ψunproj, Int64}","page":"API reference","text":"copy!(ψ1::Ψunproj, ψ2::Ψunproj, iter::Int64)\n\nPartial in-place copy assuming only particle iter changed. Does not copy slater_det_inv (the Sherman-Morrison helpers maintain the inverse on the accepted state).\n\n\n\n\n\n","title":"Base.copy!"},{"category":"method","location":"api/#Base.copy!-Tuple{Ψunproj, Ψunproj}","page":"API reference","text":"copy!(ψ1::Ψunproj, ψ2::Ψunproj)\n\nFull in-place copy. Includes slater_det_inv.\n\n\n\n\n\n","title":"Base.copy!"},{"category":"method","location":"api/#Base.copy!-Union{Tuple{T}, Tuple{T, T, Int64}} where T<:Union{Ψparton, Ψproj}","page":"API reference","text":"copy!(Ψ1, Ψ2, iter::Int64)\n\nPartial in-place copy assuming only particle iter changed (the per-particle caches plus the full coupled arrays that a single move touches).\n\n\n\n\n\n","title":"Base.copy!"},{"category":"method","location":"api/#Base.copy!-Union{Tuple{T}, Tuple{T, T}} where T<:Union{Ψparton, Ψproj}","page":"API reference","text":"copy!(Ψ1, Ψ2)\n\nFull in-place copy of one projected wavefunction into another of the same type.\n\n\n\n\n\n","title":"Base.copy!"},{"category":"method","location":"api/#CFsOnSphere.update_wavefunction!-Tuple{Union{Ψparton, Ψproj}, Float64, Float64, Int64}","page":"API reference","text":"update_wavefunction!(Ψ::Union{Ψproj,Ψparton}, θ::Float64, ϕ::Float64, iter::Int64)\n\nUpdate the projected wavefunction after moving only particle iter to (θ, ϕ). Note that because the projection couples all particles, the elementary symmetric polynomials and the whole slater_det are recomputed (no rank-1 shortcut). Modifies Ψ in place.\n\n\n\n\n\n","title":"CFsOnSphere.update_wavefunction!"},{"category":"method","location":"api/#CFsOnSphere.update_wavefunction!-Tuple{Union{Ψparton, Ψproj}, Vector{Float64}, Vector{Float64}}","page":"API reference","text":"update_wavefunction!(Ψ::Union{Ψproj,Ψparton}, θ::Vector{Float64}, ϕ::Vector{Float64})\n\nFull rebuild of the projected wavefunction Ψ from all particle positions (θ, ϕ). Modifies Ψ in place.\n\n\n\n\n\n","title":"CFsOnSphere.update_wavefunction!"},{"category":"method","location":"api/#CFsOnSphere.update_wavefunction!-Tuple{Ψunproj, Float64, Float64, Int64}","page":"API reference","text":"update_wavefunction!(ψ::Ψunproj, θ::Float64, ϕ::Float64, iter::Int64)\n\nIncremental update after moving only particle iter to (θ, ϕ): updates the Jastrow log, the iter row/column of dist_matrix, and only column iter of slater_det (single particle orbital). Modifies ψ in place.\n\n\n\n\n\n","title":"CFsOnSphere.update_wavefunction!"},{"category":"method","location":"api/#CFsOnSphere.update_wavefunction!-Tuple{Ψunproj, Vector{Float64}, Vector{Float64}}","page":"API reference","text":"update_wavefunction!(ψ::Ψunproj, θ::Vector{Float64}, ϕ::Vector{Float64})\n\nFull rebuild of ψ from all particle positions. Modifies ψ in place. (Does not refresh slater_det_inv; call initialize_inverse! afterwards if you maintain the inverse.)\n\n\n\n\n\n","title":"CFsOnSphere.update_wavefunction!"},{"category":"method","location":"api/#CFsOnSphere.calculate_ll-Tuple{Rational{Int64}, Rational{Int64}, Float64, Float64}","page":"API reference","text":"calculate_ll(l::Rational{Int}, Q::Rational{Int}, θ::Float64, φ::Float64) -> Vector{ComplexF64}\n\nEvaluate the monopole harmonics Y_Qlm(θ φ) of total angular momentum l at monopole strength Q, for all m = -l, -l+1, …, l. The returned vector is ordered by increasing m.\n\nThese are the single-particle orbitals making up the unprojected composite-fermion Slater determinant (Ψunproj), and are also used to build the fixed quasihole / quasiparticle orbital columns consumed by build_extended_slater!.\n\nThe orbital is assembled from the J_y eigenstates (see calculate_j_y_eigenstates) as a basis rotation,\n\nY_{Q,l,m}(θ,φ) = √((2l+1)/4π) · Σ_μ ⟨l,μ|_y |l,Q⟩ ⟨l,m|l,μ⟩_y e^{-iμθ} e^{imφ},\n\nwhich avoids the (discontinuous) phase of the explicit monopole-harmonic formula.\n\n\n\n\n\n","title":"CFsOnSphere.calculate_ll"},{"category":"method","location":"api/#CFsOnSphere.custom_logbinomial-Tuple{Any, Any}","page":"API reference","text":"custom_logbinomial(q1, q2) -> Float64\n\nLogarithm of the binomial coefficient C(q1, q2) via loggamma, returning -Inf when q1 - q2 < 0 (so the corresponding term drops out of projection_coeff).\n\n\n\n\n\n","title":"CFsOnSphere.custom_logbinomial"},{"category":"method","location":"api/#CFsOnSphere.generate_fourier_matrices-NTuple{4, Any}","page":"API reference","text":"generate_fourier_matrices(Qstar, N, L, Lz_list)\n\nBuild the Fourier-coefficient matrix for the Jain-Kamilla projection at total angular momentum L, effective monopole strength Qstar, and the requested Lz_list.\n\nThe JK projection binds a single vortex pair into each orbital, Jᵢ = ∏_{j≠i}(uᵢvⱼ - uⱼvᵢ), so the projected orbital is [P_LLL Y_{Q*,L,M} Jᵢ] / Jᵢ and the JK angular momentum is Q1 = (N-1)/2 (so 2Q1 = N-1 is the number of Jastrow roots), which enters the binomials of projection_coeff.\n\n\n\n\n\n","title":"CFsOnSphere.generate_fourier_matrices"},{"category":"method","location":"api/#CFsOnSphere.projection_coeff-NTuple{4, Any}","page":"API reference","text":"projection_coeff(L, Qstar, Q1, m) -> Float64\n\nThe Jain-Kamilla projection coefficient N^L_{m,Q*,Q1} (see the manuscript),\n\nN^L_{m,Q*,Q1} = C(2Q1, L-Q*) C(L-Q*, m-Q*) / C(2Q1+L+Q*+1, L-Q*)\n                · √[ (2L+1)/4π · C(2L, L+Q*) / C(2L, L+m) ],\n\nwith Q1 = (N-1)/2. Here √(2L+1)/4π is supplied separately in generate_fourier_matrices.\n\n\n\n\n\n","title":"CFsOnSphere.projection_coeff"},{"category":"method","location":"api/#CFsOnSphere.get_symmetric_polynomials!-NTuple{4, Any}","page":"API reference","text":"get_symmetric_polynomials!(dest, roots, b::Int, reg_coeffs)\n\nIn-place, regularized variant: dest[k+1] holds the degree-k elementary symmetric polynomial multiplied by ∏_{i=1}^{k} reg_coeffs[i].\n\nFor the JK projection reg_coeffs[i] = i / ((N-1) - i + 1), so the product equals 1 / C(N-1, k) = 1 / C(2Q₁, k) with Q₁ = (N-1)/2, matching the normalization ẽ in the projection coefficient N^l_{m',Q*,Q1}.\n\n\n\n\n\n","title":"CFsOnSphere.get_symmetric_polynomials!"},{"category":"method","location":"api/#CFsOnSphere.get_symmetric_polynomials!-Tuple{Any, Any, Any}","page":"API reference","text":"get_symmetric_polynomials!(dest, roots, b::Int)\n\nIn-place: fill dest[1:b+1] with the elementary symmetric polynomials of degrees 0…b in roots.\n\nArguments\n\ndest: destination array of length ≥ b+1.\nroots: the roots.\nb::Int: maximum ESP degree to compute.\n\n\n\n\n\n","title":"CFsOnSphere.get_symmetric_polynomials!"},{"category":"method","location":"api/#CFsOnSphere.get_symmetric_polynomials-Tuple{Any, Any}","page":"API reference","text":"get_symmetric_polynomials(roots, b)\n\nAllocating reference implementation (not exported): returns a length-b+1 vector whose k+1-th entry is the elementary symmetric polynomial of degree k in roots. Used to cross-check the in-place versions.\n\n\n\n\n\n","title":"CFsOnSphere.get_symmetric_polynomials"},{"category":"method","location":"api/#CFsOnSphere.legendre_polynomials!-Tuple{Any, Any, Int64}","page":"API reference","text":"legendre_polynomials!(res, x, kmax::Int64)\n\nCompute the Legendre polynomials up to degree kmax at point x, storing the results in the pre-allocated array res (length kmax+1); res[k] holds P_{k-1}(x).\n\nUses the recurrence Pₙ₊₁(x) = ((2n+1) x Pₙ(x) - n Pₙ₋₁(x)) / (n+1). Modifies res in-place.\n\n\n\n\n\n","title":"CFsOnSphere.legendre_polynomials!"},{"category":"method","location":"api/#CFsOnSphere.calculate_j_y_eigenstates-Tuple{Rational}","page":"API reference","text":"calculate_j_y_eigenstates(l::Rational) -> Dict{NTuple{3,Rational{Int64}},ComplexF64}\n\nReturn a dictionary d[(μ, m1, m2)] = ⟨l,m1|l,μ⟩_y · conj(⟨l,m2|l,μ⟩_y), i.e. the product of J_y-eigenvector components needed to build rotation / Wigner-d matrix elements at angular momentum l. The result is memoised on disk in tempdir().\n\n\n\n\n\n","title":"CFsOnSphere.calculate_j_y_eigenstates"},{"category":"method","location":"api/#CFsOnSphere.u_v_generator-Tuple{Any, Any}","page":"API reference","text":"u_v_generator(θ, ϕ)\n\nConvert spherical coordinates (θ, ϕ) to the spinor (CP¹) coordinates\n\nu = cos(θ/2) exp(+i ϕ/2),   v = sin(θ/2) exp(-i ϕ/2).\n\nWorks for scalar inputs (returns a Tuple{ComplexF64,ComplexF64}) and for vector inputs (returns a tuple of Vector{ComplexF64}), since the body broadcasts.\n\n\n\n\n\n","title":"CFsOnSphere.u_v_generator"},{"category":"method","location":"api/#CFsOnSphere.build_extended_slater!-Tuple{Matrix{ComplexF64}, Union{Ψproj, Ψunproj}, AbstractMatrix{ComplexF64}}","page":"API reference","text":"build_extended_slater!(Sfull, ψ, qh_columns) -> LU\n\nFill the preallocated (N+k)×(N+k) matrix Sfull with the electron Slater block of ψ (first N columns) and k fixed quasihole / quasiparticle orbital columns qh_columns (built from calculate_ll), and return its LU factorization.\n\nWorks for Ψproj (recompute the LU each step — the projection has no rank-1 shortcut) and for Ψunproj. Quasihole amplitudes follow from transpose(LU) \\ e_end.\n\n\n\n\n\n","title":"CFsOnSphere.build_extended_slater!"},{"category":"method","location":"api/#CFsOnSphere.initialize_inverse!-Tuple{Ψunproj}","page":"API reference","text":"initialize_inverse!(ψ::Ψunproj)\n\nCompute ψ.slater_det_inv = inv(ψ.slater_det) (O(N³)). Call once after the first full update_wavefunction!; requires a square (closed-shell) slater_det.\n\n\n\n\n\n","title":"CFsOnSphere.initialize_inverse!"},{"category":"method","location":"api/#CFsOnSphere.slater_det_ratio-Tuple{Ψunproj, Ψunproj, Int64}","page":"API reference","text":"slater_det_ratio(ψcurrent::Ψunproj, ψnext::Ψunproj, iter::Int) -> ComplexF64\n\nThe determinant ratio det(Sₙₑₓₜ)/det(S_current) for a move of particle iter, computed in O(N) from the maintained inverse of ψcurrent and the new column of ψnext: Sinv[iter, :] · Sₙₑₓₜ[:, iter].\n\n\n\n\n\n","title":"CFsOnSphere.slater_det_ratio"},{"category":"method","location":"api/#CFsOnSphere.update_inverse!-Tuple{Ψunproj, Ψunproj, Int64, ComplexF64, Vector{ComplexF64}}","page":"API reference","text":"update_inverse!(ψcurrent::Ψunproj, ψnext::Ψunproj, iter::Int, det_ratio::ComplexF64, temp::Vector{ComplexF64})\n\nRank-1 Sherman-Morrison update of ψcurrent.slater_det_inv for an accepted move of particle iter to ψnext (O(N²)). det_ratio is the value returned by slater_det_ratio; temp is an N-length scratch vector. Call before copy!(ψcurrent, ψnext, iter).\n\n\n\n\n\n","title":"CFsOnSphere.update_inverse!"},{"category":"method","location":"api/#CFsOnSphere.cf_ground_state_lm-Tuple{Int64, Int64, Int64}","page":"API reference","text":"cf_ground_state_lm(N, n, p) -> (Qstar, l_m_list)\n\nEffective monopole strength and occupied (L, Lz) orbitals for the CF ground state of N electrons in n filled Λ-levels at filling ν = n/(pn+1), where p is the Jastrow power (the number of attached vortices, even) that you pass on to Ψproj/Ψunproj. Qstar = (N//n - n)//2 (independent of p), with all Lz filled in Λ-levels |Qstar| … |Qstar|+|n|-1.\n\n\n\n\n\n","title":"CFsOnSphere.cf_ground_state_lm"},{"category":"method","location":"api/#CFsOnSphere.cf_quasihole_lm-Tuple{Int64, Int64, Int64}","page":"API reference","text":"cf_quasihole_lm(N, n, p) -> (Qstar, l_m_list)\n\nAs cf_ground_state_lm but with one quasihole: the top orbital (Lqh, Lqh) of the highest occupied Λ-level (Lqh = |Qstar|+|n|-1) is removed.\n\n\n\n\n\n","title":"CFsOnSphere.cf_quasihole_lm"},{"category":"method","location":"api/#CFsOnSphere.cf_quasiparticle_lm-Tuple{Int64, Int64, Int64}","page":"API reference","text":"cf_quasiparticle_lm(N, n, p) -> (Qstar, l_m_list)\n\nAs cf_ground_state_lm but with one quasiparticle: the orbital (Lqp, Lqp) in the next Λ-level (Lqp = |Qstar|+|n|) is added.\n\n\n\n\n\n","title":"CFsOnSphere.cf_quasiparticle_lm"},{"category":"method","location":"api/#CFsOnSphere.arm_parameters-Tuple{Float64, Float64}","page":"API reference","text":"arm_parameters(ideal_acceptance_ratio::Float64, r::Float64) -> (a, b)\n\nPrecompute the parameters (a, b) of the ARM (acceptance-rate monitoring) step-size scheme for a target acceptance ratio, via a fixed-point iteration.\n\n\n\n\n\n","title":"CFsOnSphere.arm_parameters"},{"category":"method","location":"api/#CFsOnSphere.arm_scale_factor-NTuple{4, Any}","page":"API reference","text":"arm_scale_factor(p, p_i, a, b) -> Float64\n\nMultiplicative step-size rescale factor given current acceptance p, target p_i, and ARM parameters (a, b): > 1 when acceptance is too high, < 1 when too low.\n\n\n\n\n\n","title":"CFsOnSphere.arm_scale_factor"},{"category":"method","location":"api/#CFsOnSphere.construct_det_ratios-Tuple{Vector{Int64}, Vector{Vector{Int64}}}","page":"API reference","text":"construct_det_ratios(denominator_rows, numerator_rows)\n\nBuild a closure helper!(res, S, Sinv) computing the determinant ratios det(S[numerator_rows[i], :]) / det(S[denominator_rows, :]) efficiently by reducing to the sub-determinant over rows not shared with the denominator. Sinv is the inverse of S[denominator_rows, :]. All numerator row-sets must have the same length as denominator_rows; matrix elements are assumed ComplexF64.\n\n\n\n\n\n","title":"CFsOnSphere.construct_det_ratios"},{"category":"method","location":"api/#CFsOnSphere.gibbs_thermalization!-Union{Tuple{T}, Tuple{Any, T, T, Vector{Float64}, Vector{Float64}, Vector{Float64}, Vector{Float64}, Float64, Function, Int64}} where T<:Union{ΨoneLL, Ψparton, Ψproj, Ψunproj}","page":"API reference","text":"gibbs_thermalization!(RNG, Ψcurrent, Ψnext, θcurrent, ϕcurrent, θnext, ϕnext, σinit, logpdf, num_thermalization)\n\nThermalize a single-particle Gibbs / Metropolis-Hastings walk targeting logpdf(Ψ) (a real scalar), tuning the step size σ toward a 50% acceptance rate via the ARM scheme. Works for any wavefunction type (Ψproj, Ψparton, ΨoneLL, Ψunproj).\n\nReturns (sampling_iter, σ, δt_therm, acceptance_rate).\n\n\n\n\n\n","title":"CFsOnSphere.gibbs_thermalization!"},{"category":"method","location":"api/#CFsOnSphere.proposal-Tuple{Any, Float64, Float64, Float64}","page":"API reference","text":"proposal(RNG, θcurrent::Float64, ϕcurrent::Float64, σ::Float64) -> (θnew, ϕnew)\n\nPropose a new point on the sphere by rotating the current position along a great circle by a Gaussian angle δθ ~ N(0, σ) in a uniformly random tangent direction.\n\nThe current position r̂ is taken as a Cartesian unit vector; a random 3D Gaussian is projected onto the tangent plane at r̂ and normalized to give the direction ê, and the new point is r̂ cos(δθ) + ê sin(δθ). This proposal is isotropic about the current point (hence symmetric, preserving detailed balance). Returns (θ, ϕ) in the same convention as rand_θ_ϕ_gen.\n\n\n\n\n\n","title":"CFsOnSphere.proposal"},{"category":"method","location":"api/#CFsOnSphere.rand_θ_ϕ_gen-Tuple{Any, Int64}","page":"API reference","text":"rand_θ_ϕ_gen(RNG, n_samples::Int) -> (θlist, ϕlist)\n\nGenerate n_samples points uniformly on the unit sphere, returned as polar angles θ ∈ [0, π] and azimuthal angles ϕ ∈ (-π, π] (via atan). Uses the Marsaglia method (normalize 3D Gaussians).\n\n\n\n\n\n","title":"CFsOnSphere.rand_θ_ϕ_gen"},{"category":"method","location":"api/#CFsOnSphere.update_density!-Tuple{Vector{Float64}, Vector{Float64}, Vector{Float64}, Vector{Float64}, Matrix{Float64}}","page":"API reference","text":"update_density!(θmesh, ϕmesh, θcurrent, ϕcurrent, accumulated_density)\n\nAccumulate a 2D (θ, ϕ) density histogram in place.\n\n\n\n\n\n","title":"CFsOnSphere.update_density!"},{"category":"method","location":"api/#CFsOnSphere.update_density!-Tuple{Vector{Float64}, Vector{Float64}, Vector{Float64}}","page":"API reference","text":"update_density!(θmesh, θcurrent, accumulated_density)\n\nAccumulate a 1D polar-angle density histogram in place: for each θ in θcurrent, increment the bin of accumulated_density located by searchsortedfirst(θmesh, θ) - 1.\n\n\n\n\n\n","title":"CFsOnSphere.update_density!"},{"category":"section","location":"theory/#Theory-and-citation","page":"Theory & citation","text":"","title":"Theory & citation"},{"category":"section","location":"theory/#The-method","page":"Theory & citation","text":"The projection implemented here is the quaternion/rotation reformulation of the Jain–Kamilla projection introduced in:\n\nM. Gattu and J. K. Jain, Unlocking New Regimes in Fractional Quantum Hall Effect with Quaternions, Phys. Rev. Lett. 134, 156501 (2025).\n\nIn this formulation each lowest-Landau-level-projected composite-fermion orbital is a sum over a Wigner-D matrix times elementary symmetric polynomials of the Jastrow \"vortex ratios\" — see Physics background for the working equations. This is both cheaper and more numerically stable than the traditional mixed-derivative projection.","title":"The method"},{"category":"section","location":"theory/#The-single-vortex-pair-projection","page":"Theory & citation","text":"The package binds a single vortex pair inside the Jain–Kamilla projection (Q_1 = (N-1)2), and reaches the full Jain sequence nu = n(pn+1) through the outer holomorphic Jastrow factor (the global Jastrow power p; see Higher fillings).\n\nBinding more than one pair inside the projection is mathematically valid at integer multiplicity but unnecessary and fragile: the outer-Jastrow construction reaches every filling using the safe single-pair projection, while a higher multiplicity makes the elementary symmetric polynomials develop higher-order self-poles that are well-behaved only at integer multiplicity (and diverge for fractional values). The full derivation and the clustering analysis behind this choice are kept in the repository:\n\nderivation_jktype.tex — the generalized projection derivation;\nmathematica/ — the short-distance (clustering) analysis, with README.md.","title":"The single-vortex-pair projection"},{"category":"section","location":"theory/#How-to-cite","page":"Theory & citation","text":"If you use this package in published work, please cite the method paper:\n\n@article{PhysRevLett.134.156501,\n  title   = {Unlocking New Regimes in Fractional Quantum Hall Effect with Quaternions},\n  author  = {Gattu, Mytraya and Jain, J. K.},\n  journal = {Phys. Rev. Lett.},\n  volume  = {134},\n  issue   = {15},\n  pages   = {156501},\n  numpages = {9},\n  year    = {2025},\n  month   = {Apr},\n  publisher = {American Physical Society},\n  doi     = {10.1103/PhysRevLett.134.156501},\n  url     = {https://link.aps.org/doi/10.1103/PhysRevLett.134.156501}\n}\n\nand link to the software repository, github.com/mytraya-gattu/CompositeFermions.","title":"How to cite"},{"category":"section","location":"tutorials/07_under_the_hood/#7.-Under-the-hood:-orbitals-and-symmetric-polynomials","page":"7 — Under the hood","text":"This last tutorial opens up the two ingredients the projected wavefunction is assembled from: the single-particle monopole harmonics, and the elementary symmetric polynomials (ESP) of the Jastrow vortex ratios. Understanding these makes the rest of the code transparent. See Physics background for the equations.\n\nusing CFsOnSphere\nusing Random\nusing LinearAlgebra\nLinearAlgebra.BLAS.set_num_threads(1)\nusing Plots\ngr()","title":"7. Under the hood: orbitals and symmetric polynomials"},{"category":"section","location":"tutorials/07_under_the_hood/#Monopole-harmonics","page":"7 — Under the hood","text":"calculate_ll(l, Q, θ, φ) returns the monopole harmonics Y_Qlm(θφ) for all m = -ldotsl at once. These are the orbitals of the unprojected determinant. Here are the lowest-Landau-level (l = Q) orbitals at Q = 2, plotted as Y_Qlm(θ0)^2 along a meridian — each m is a ring of latitude.\n\nQ = 2 // 1\nl = 2 // 1                      # l = Q : lowest Landau level\nθs = range(0, π; length = 200)\nY = [calculate_ll(l, Q, θ, 0.0) for θ in θs]    # each entry is a length-(2l+1) vector over m\n\nplt = plot(xlabel = \"θ\", ylabel = \"|Y(θ)|²\",\n           title = \"LLL monopole harmonics  Y_{2,2,m}\", legend = :top)\nfor (idx, m) in enumerate(-l:l)\n    plot!(plt, θs, [abs2(Yθ[idx]) for Yθ in Y]; lw = 2, label = \"m = $(Int(m))\")\nend\nplt","title":"Monopole harmonics"},{"category":"section","location":"tutorials/07_under_the_hood/#Elementary-symmetric-polynomials","page":"7 — Under the hood","text":"The projection's heavy lifting is contracting Wigner-D matrices with the ESP of the vortex ratios. get_symmetric_polynomials! fills dest[k+1] with the degree-k ESP e_k of the supplied roots. We can check it against the textbook definition: the e_k are exactly the coefficients of prod_i (1 + r_i x).\n\nroots = ComplexF64[0.3 + 0.1im, -0.5 + 0.2im, 0.7 - 0.4im, 0.1 + 0.6im]\nb = length(roots)\nesp = zeros(ComplexF64, b + 1)\nget_symmetric_polynomials!(esp, roots, b)\n\n# Ground truth: the eₖ are the coefficients of ∏(1 + rᵢ x), expanded directly.\nfunction esp_direct(roots)\n    poly = ComplexF64[1.0]\n    for r in roots\n        poly = vcat(poly, 0im) .+ vcat(0im, r .* poly)\n    end\n    return poly\nend\npoly = esp_direct(roots)\n\nprintln(\"degree   e_k (package)            e_k (direct ∏(1+rᵢx))\")\nfor k in 0:b\n    println(rpad(k, 8), rpad(round(esp[k+1], digits=4), 26), round(poly[k+1], digits=4))\nend\nprintln(\"\\nmax |difference| = \", maximum(abs.(esp .- poly)))\n\nThe two agree to machine precision. In the projection the roots are the vortex ratios (r_i^-1cdot r_j)_S  (r_i^-1cdot r_j)_A for j neq i, and a regularized variant of the same routine divides e_k by binomN-1k on the fly — that is the normalization tilde e appearing in the projection coefficient. From here, the projected orbital is just a Wigner-D-weighted sum of these polynomials, exactly as in Physics background.\n\n\n\nThis page was generated using Literate.jl.","title":"Elementary symmetric polynomials"},{"category":"section","location":"tutorials/05_partons/#5.-Parton-states","page":"5 — Parton states","text":"Parton wavefunctions generalize composite fermions: the electron is split into several fictitious \"partons\", each in its own integer-quantum-Hall-like state, and the physical wavefunction is the product of the parton determinants (projected to the LLL). The package represents such a state with Ψparton, which stores one Slater matrix whose row blocks are the different parton species (tracked by ψ.trackers).\n\nThis tutorial shows how to build a parton state and evaluate its amplitude. For a complete Monte Carlo parton sampler, see examples/sampler_single_state_parton.jl in the repository.\n\nusing CFsOnSphere\nusing Random\nusing LinearAlgebra\nLinearAlgebra.BLAS.set_num_threads(1)","title":"5. Parton states"},{"category":"section","location":"tutorials/05_partons/#Constructing-a-parton-state","page":"5 — Parton states","text":"Ψparton takes a vector of effective monopole strengths Qstars (one per parton species) and a matching vector of orbital lists l_m_lists, each containing N orbitals (so every species forms an Ntimes N block). Here we build a simple two-species state; the species share the same Lambda-level structure for illustration.\n\nN = 6\nQstar, l_m_list = cf_ground_state_lm(N, 1, 2)     # one filled Λ-level, N orbitals\nQstars     = [Qstar, Qstar]\nl_m_lists  = [l_m_list, l_m_list]\np = 2\n\nψ = Ψparton(Qstars, p, N, l_m_lists)\n@show length(ψ.trackers) size(ψ.slater_det);\nnothing #hide\n\nψ.trackers[s] gives the rows of ψ.slater_det belonging to species s — each an Ntimes N block.","title":"Constructing a parton state"},{"category":"section","location":"tutorials/05_partons/#Evaluating-the-amplitude","page":"5 — Parton states","text":"Updating is identical to the projected wavefunction. The parton amplitude is the product of the per-species block determinants times the Jastrow factor, so the log-amplitude is the sum of the block logdets:\n\nrng = MersenneTwister(8)\nθ, ϕ = rand_θ_ϕ_gen(rng, N)\nupdate_wavefunction!(ψ, θ, ϕ)\n\nlogamp = sum(logdet(ψ.slater_det[tr, :]) for tr in ψ.trackers) + ψ.jastrow_factor_log\nprintln(\"number of species = \", length(ψ.trackers))\nprintln(\"Re(log Ψ) = \", round(real(logamp), digits=6))\nprintln(\"finite amplitude: \", isfinite(real(logamp)))\n\nTo sample a parton state you would use this product-of-determinants amplitude as the log-pdf inside the same Metropolis–Hastings–Gibbs loop as the earlier tutorials (driving it with gibbs_thermalization!, which already accepts Ψparton).\n\n\n\nThis page was generated using Literate.jl.","title":"Evaluating the amplitude"},{"category":"section","location":"physics/#Physics-background","page":"Physics background","text":"This page is a gentle, self-contained primer on the physics the package implements. It assumes familiarity with quantum mechanics but not with the fractional quantum Hall effect (FQHE). If you just want to run code, skim the notation table and jump to the Tutorials.","title":"Physics background"},{"category":"section","location":"physics/#The-lowest-Landau-level-on-a-sphere","page":"Physics background","text":"In a strong magnetic field, two-dimensional electrons occupy quantized Landau levels. At low filling only the lowest Landau level (LLL) matters, and the kinetic energy is quenched — all the physics is in the electron–electron interaction projected into the LLL.\n\nHaldane's sphere geometry places N electrons on a sphere with a magnetic monopole of strength Q at its centre, so the total flux through the surface is 2Q flux quanta (2Q is a non-negative integer by Dirac quantization). Single-particle states are monopole harmonics Y_Qlm, labelled by angular momentum l = Q Q+1 dots (the LLL is l = Q) and m = -ldotsl. The sphere has no edge, which makes it ideal for extracting bulk properties.\n\nEach electron's position is a point on the sphere (thetaphi), encoded by a spinor\n\nu = cos(theta2)e^iphi2 qquad v = sin(theta2)e^-iphi2","title":"The lowest Landau level on a sphere"},{"category":"section","location":"physics/#Composite-fermions","page":"Physics background","text":"The composite fermion (CF) is the emergent particle of the FQHE: an electron bound to an even number p of vortices (quantized flux). The bound vortices screen part of the magnetic field, so composite fermions experience a reduced effective flux\n\n2Q^star = 2Q - p(N-1)\n\nComposite fermions then fill n of their own effective Landau levels (called Lambda-levels), which produces the famous Jain sequence of filling fractions\n\nnu = fracnpn + 1\n\nThe CF wavefunction is a Slater determinant of CF orbitals times a Jastrow factor that attaches the p vortices:\n\nPsi_nu = mathcalP_mathrmLLLPhi_nprod_jk(u_j v_k - u_k v_j)^p\n\nwhere Phi_n is the Slater determinant of n filled levels at effective flux 2Q^star, and mathcalP_mathrmLLL projects the product back into the lowest Landau level.\n\nnote: The meaning of `p` in this package\nThroughout the code, p is the power of the Jastrow factor — the total number of vortices (flux quanta) attached to each electron. It is even: p = 2 attaches one vortex pair (the textbook 2tilde p flux with tilde p = 1), p = 4 attaches two pairs, and so on. In terms of p the filling is nu = n(pn+1):p n nu\n2 1 1/3\n2 2 2/5\n2 3 3/7\n4 1 1/5\n4 2 2/9So the nu = 13 Laughlin state is p = 2, not p = 1.","title":"Composite fermions"},{"category":"section","location":"physics/#The-Jain–Kamilla-projection","page":"Physics background","text":"Carrying out mathcalP_mathrmLLL exactly is exponentially expensive. The Jain–Kamilla (JK) projection makes it tractable by projecting one electron's orbital at a time. This package uses the quaternion/rotation reformulation of JK projection (Phys. Rev. Lett. 134, 156501 (2025)), in which each projected orbital becomes\n\nfracmathcalP_mathrmLLL Y_Q^starlm(Omega_i) J_iJ_i\n= sum_m N^l_mQ^starQ_1mathcalD^l_mm(Omega_i)tilde e_m-Q^star(X_i)\nqquad J_i = prod_jneq i(u_i v_j - u_j v_i)\n\na sum over a Wigner-D matrix mathcalD^l_mm times elementary symmetric polynomials tilde e_r of the \"vortex ratios\" X_i. This is the central object the code evaluates — far cheaper and more numerically stable than the traditional mixed-derivative projection.\n\nThe projection binds a single vortex pair per orbital, so the JK angular momentum is Q_1 = (N-1)2.","title":"The Jain–Kamilla projection"},{"category":"section","location":"physics/#Reaching-higher-fillings:-the-outer-Jastrow","page":"Physics background","text":"You might expect more vortices (larger p) to require binding more pairs inside the projection. It does not. The projection always binds a single vortex pair, and the remaining vortices come from the Jastrow power as a holomorphic prefactor that stays in the LLL automatically:\n\nPsi = Phi_1^p-2 mathcalP_mathrmLLLleftPhi_nPhi_1^2right\nqquad Phi_1 = prod_jk(u_j v_k - u_k v_j)\n\nSo nothing special is needed for higher fillings — just pass the desired total vortex count p. For example nu = 29 (n = 2, two pairs Rightarrow p = 4) is simply Ψproj(Qstar, 4, N, l_m_list). The higher-fillings tutorial demonstrates this.\n\nnote: Why a single bound pair?\nBinding more than one pair inside the projection is unnecessary (the outer Jastrow reaches every filling) and numerically fragile. See Theory & citation for the analysis.","title":"Reaching higher fillings: the outer Jastrow"},{"category":"section","location":"physics/#Notation-in-code","page":"Physics background","text":"Symbol Code Meaning\nQ^star Qstar::Rational{Int64} effective monopole strength, Q^star = Q - p(N-1)2\np p::Int64 Jastrow power prod(u_jv_k-u_kv_j)^p; even (= 2× vortex pairs); nu = n(pn+1)\n(l m) l_m_list::Vector{NTuple{2,Rational}} occupied (L L_z) orbitals (the filled Lambda-levels)\nn n number of filled Lambda-levels\nN system_size number of composite fermions\n(uv) U, V spinor coordinates of each particle\n\nThe convenience builders cf_ground_state_lm, cf_quasihole_lm, and cf_quasiparticle_lm construct (Qstar, l_m_list) for the standard states so you rarely set them by hand.","title":"Notation in code"},{"category":"section","location":"validation/#Validation","page":"Validation","text":"The package is covered by a deterministic Julia test suite and a cross-language check against the C++ port.","title":"Validation"},{"category":"section","location":"validation/#Julia-test-suite","page":"Validation","text":"test/runtests.jl runs with a fixed RNG seed and covers:\n\nrandom configuration generation and density accumulation;\nΨproj full-vs-incremental update consistency (a sequence of single-particle moves must reproduce a full rebuild);\nthe Λ-level builders (cf_ground_state_lm and friends);\nthe elementary symmetric polynomials, checked to all orders against an independent ground-truth expansion of prod_i(1 + r_i x);\nthe Metropolis proposal convention and isotropy;\nΨunproj orbitals vs calculate_ll and incremental updates;\nSherman–Morrison inverse tracking (slater_det_inv matches a fresh inverse; the determinant ratio matches brute force);\nthe non-square extended Slater determinant for quasihole/quasiparticle amplitudes.\n\nRun it with:\n\nusing Pkg; Pkg.test()","title":"Julia test suite"},{"category":"section","location":"validation/#C-cross-check","page":"Validation","text":"The C++ port (cpp/) is validated against Julia reference data generated by cpp/reference/dump_reference.jl and cpp/reference/dump_logpsi.jl:\n\ntest_cfsonsphere — native structural tests mirroring the Julia suite;\ntest_reference — element-wise cross-check of the Slater matrix, projection coefficients, and Jastrow factor against Julia CSVs to le 10^-12;\ntest_logpsi — a large cross-check of logPsi across many systems (sizes 4→32, Lambda-levels 1→5, projected and unprojected) and 1000 shared configurations each.\n\ncd cpp\ncmake -B build -DCMAKE_PREFIX_PATH=/opt/homebrew\ncmake --build build\nctest --test-dir build --output-on-failure\n\nBecause std::mt19937_64 differs from Julia's RNG, Monte Carlo chains agree only statistically, but the deterministic wavefunction values match to machine precision.","title":"C++ cross-check"},{"category":"section","location":"tutorials/04_unprojected_fast/#4.-Unprojected-states-and-fast-Sherman–Morrison-updates","page":"4 — Unprojected + Sherman–Morrison","text":"The unprojected CF wavefunction detY_Q^starlm(Omega_i)prod(u_jv_k-u_kv_j)^p is built from single-particle monopole-harmonic orbitals (Ψunproj). Because moving one particle changes only one column of the Slater determinant, the determinant ratio and inverse can be tracked by a rank-1 Sherman–Morrison update — turning the per-step cost from O(N^3) (a fresh logdet) into O(N) for the acceptance ratio plus O(N^2) for the inverse refresh.\n\n(This shortcut is not available for the projected Ψproj: there the elementary symmetric polynomials couple all particles, so one move changes every column. See Architecture.)\n\nusing CFsOnSphere\nusing Random\nusing LinearAlgebra\nLinearAlgebra.BLAS.set_num_threads(1)\nusing Plots\ngr()","title":"4. Unprojected states and fast Sherman–Morrison updates"},{"category":"section","location":"tutorials/04_unprojected_fast/#The-Sherman–Morrison-sampling-loop","page":"4 — Unprojected + Sherman–Morrison","text":"We thermalize with the generic driver (which uses logdet), then switch to the accelerated inner loop: maintain the inverse on the accepted state with initialize_inverse!, get each acceptance ratio from slater_det_ratio, and on acceptance apply update_inverse! before copying the state across.\n\nfunction sample_unprojected(N, n, p; seed = 4, n_therm = 100_000, n_steps = 1_000_000, n_bins = 30)\n    Qstar, l_m_list = cf_ground_state_lm(N, n, p)\n    system_size = length(l_m_list)\n    rng = MersenneTwister(seed)\n\n    ψ      = Ψunproj(Qstar, p, system_size, l_m_list)\n    ψ_next = Ψunproj(Qstar, p, system_size, l_m_list)\n    logpdf(ψ) = 2.0 * real(logdet(ψ.slater_det) + ψ.jastrow_factor_log)\n\n    θ, ϕ = rand_θ_ϕ_gen(rng, system_size)\n    θ_next, ϕ_next = copy(θ), copy(ϕ)\n    σ = π / sqrt(12)\n    sampling_iter, σ, _, _ = gibbs_thermalization!(rng, ψ, ψ_next, θ, ϕ, θ_next, ϕ_next, σ, logpdf, n_therm)\n\n    initialize_inverse!(ψ)                  # maintain the inverse on the accepted state\n    temp = zeros(ComplexF64, system_size)\n\n    θmesh = acos.(LinRange(1.0, -1.0, n_bins + 1))\n    Agrid = 2π .* (cos.(θmesh[1:end-1]) .- cos.(θmesh[2:end]))\n    density = zeros(Float64, n_bins)\n\n    accepted = 0\n    for _ in 1:n_steps\n        i = sampling_iter\n        θ_next[i], ϕ_next[i] = proposal(rng, θ[i], ϕ[i], σ)\n        update_wavefunction!(ψ_next, θ_next[i], ϕ_next[i], i)\n\n        det_ratio = slater_det_ratio(ψ, ψ_next, i)            # O(N)\n        δ = 2.0 * real(log(det_ratio) + ψ_next.jastrow_factor_log - ψ.jastrow_factor_log)\n\n        if δ >= log(rand(rng))\n            θ[i], ϕ[i] = θ_next[i], ϕ_next[i]\n            update_inverse!(ψ, ψ_next, i, det_ratio, temp)    # O(N²), before copy!\n            copy!(ψ, ψ_next, i)\n            accepted += 1\n        else\n            θ_next[i], ϕ_next[i] = θ[i], ϕ[i]\n            copy!(ψ_next, ψ, i)\n        end\n\n        update_density!(θmesh, θ, density)\n        sampling_iter = mod(sampling_iter, system_size) + 1\n    end\n\n    θc = 0.5 .* (θmesh[1:end-1] .+ θmesh[2:end])\n    return θc, density ./ n_steps ./ Agrid, accepted / n_steps\nend\n\nWe sample the unprojected nu = 25 state (n = 2, p = 2) for N = 8 electrons (a closed shell, so the determinant is square and the inverse can be maintained).\n\nθc, dens, accept = sample_unprojected(8, 2, 2)\n@show accept;\nnothing #hide\n\nLocal filling nu(theta) = 2pin(theta)Q_textshift with Q_textshift = N2nu; the bulk plateau sits at nu = 25, shown against the y=0 baseline.\n\nN      = 8\nν      = 2 / 5\nQshift = N / (2ν)\nνθ     = dens .* (2π / Qshift)\n\nplot(θc, νθ; lw=2, marker=:circle, ms=3, label=\"measured\", ylims=(0.0, 0.6),\n     xlabel=\"θ\", ylabel=\"local filling  ν(θ)\", title=\"Unprojected ν = 2/5 (N = 8, Sherman–Morrison)\")\nhline!([ν]; ls=:dash, label=\"ν = 2/5\")\n\n\n\nThis page was generated using Literate.jl.","title":"The Sherman–Morrison sampling loop"},{"category":"section","location":"#CFsOnSphere.jl","page":"Home","text":"Composite-fermion wavefunctions on the sphere, for the fractional quantum Hall effect.\n\nCFsOnSphere builds Jain–Kamilla projected and unprojected composite-fermion (CF) and parton wavefunctions on the Haldane sphere, and samples them with a Metropolis–Hastings–Gibbs Monte Carlo walk to compute densities, pair correlations, energies, and overlaps. The projection uses the quaternion/rotation reformulation of Jain–Kamilla projection (Phys. Rev. Lett. 134, 156501 (2025)), which is far cheaper and more numerically stable than the traditional mixed-derivative approach.\n\nIf you are new here, read the Physics background for the concepts and notation, then work through the Tutorials.","title":"CFsOnSphere.jl"},{"category":"section","location":"#Installation","page":"Home","text":"The package is not yet in the General registry, so install it from the repository:\n\nusing Pkg\nPkg.add(url=\"https://github.com/mytraya-gattu/CompositeFermions.git\")\n\nor, for development, clone it and Pkg.develop the checkout:\n\ngit clone https://github.com/mytraya-gattu/CompositeFermions.git\n\nusing Pkg; Pkg.develop(path=\"CompositeFermions\")","title":"Installation"},{"category":"section","location":"#Quickstart","page":"Home","text":"A complete program that builds the nu = 13 composite-fermion ground state, samples it with Monte Carlo, and plots its density on the sphere. We go through it one block at a time — each block is independent and links to where it is explained in full.\n\n1. Choose the state. A Jain state is fixed by three integers: the number of particles N, the number of filled Λ-levels n, and the Jastrow power p (the number of attached vortices, even). They set the filling nu = n(pn+1). cf_ground_state_lm returns the effective monopole strength Qstar and the occupied (L L_z) orbitals — see Physics background for what these mean.\n\nusing CFsOnSphere, Random, LinearAlgebra, Statistics\n\nN, n, p = 10, 1, 2                          # n=1, p=2  →  ν = 1/3\nQstar, l_m_list = cf_ground_state_lm(N, n, p)\n\n2. Define the sampling weight. We sample positions with probability Psi^2. The walker works with a log probability, and Psi^2 = det^2textJastrow^2, so the log-pdf reads the projected Slater determinant (Ψproj) and the cached Jastrow log off the wavefunction object:\n\nlogpdf(ψ) = 2.0 * real(logdet(ψ.slater_det) + ψ.jastrow_factor_log)\n\n3. Run one Metropolis chain. Allocate two wavefunction buffers (current + proposed), thermalize with gibbs_thermalization! (which also tunes the step size σ), then sweep: proposal moves one particle, update_wavefunction! updates that column, and on accept/reject we keep the better state. update_density! accumulates an equal-area theta histogram. (This is exactly the loop from Tutorial 1, wrapped in a function.)\n\nfunction run_chain(seed; n_therm = 100_000, n_steps = 1_000_000, n_bins = 100)\n    rng = MersenneTwister(seed)\n    ψ, ψn = Ψproj(Qstar, p, N, l_m_list), Ψproj(Qstar, p, N, l_m_list)\n    θ, ϕ = rand_θ_ϕ_gen(rng, N); θn, ϕn = copy(θ), copy(ϕ)\n    it, σ, _, _ = gibbs_thermalization!(rng, ψ, ψn, θ, ϕ, θn, ϕn, π/sqrt(12), logpdf, n_therm)\n\n    θmesh = acos.(LinRange(1.0, -1.0, n_bins + 1))     # equal-area bin edges\n    dens = zeros(n_bins); lpc = logpdf(ψ)\n    for _ in 1:n_steps\n        θn[it], ϕn[it] = proposal(rng, θ[it], ϕ[it], σ)\n        update_wavefunction!(ψn, θn[it], ϕn[it], it)\n        if logpdf(ψn) - lpc >= log(rand(rng))\n            θ[it], ϕ[it] = θn[it], ϕn[it]; copy!(ψ, ψn, it); lpc = logpdf(ψ)\n        else\n            θn[it], ϕn[it] = θ[it], ϕ[it]; copy!(ψn, ψ, it)\n        end\n        update_density!(θmesh, θ, dens)\n        it = mod(it, N) + 1\n    end\n    Agrid = 2π .* (cos.(θmesh[1:end-1]) .- cos.(θmesh[2:end]))\n    return θmesh, dens ./ n_steps ./ Agrid\nend\n\n4. Average several independent chains. Independent chains (different seeds) reduce the Monte Carlo error; ten chains of a million steps give a smooth estimate.\n\nresults = [run_chain(seed) for seed in 1:10]          # 10 independent chains\nθmesh   = results[1][1]\nθc      = 0.5 .* (θmesh[1:end-1] .+ θmesh[2:end])     # bin centres\ndensity = mean(last.(results))                        # number density on the unit sphere\n\n5. Convert to the local filling and plot. It is natural to report the density in magnetic units — the local filling nu(theta) — whose bulk value is the filling fraction nu itself. With the monopole strength Q_textshift = N2nu for this state, nu(theta) = 2pin(theta)Q_textshift. Plotting from 0 keeps the flat plateau in context (rather than zooming into the per-mille fluctuations).\n\nusing Plots\nν      = n / (p*n + 1)                 # target filling = 1/3\nQshift = N / (2ν)                      # monopole strength for this filling\nνθ     = density .* (2π / Qshift)      # local filling ν(θ): flat value = ν\n\nplot(θc, νθ; lw = 2, xlabel = \"θ\", ylabel = \"local filling  ν(θ)\", ylims = (0.0, 0.5),\n     label = \"N=10, 10 chains × 10⁶ steps\")\nhline!([ν]; ls = :dash, label = \"ν = 1/3\")\n\nRunning the program above produces:\n\n(Image: Local filling ν(θ) of the ν = 1/3 composite-fermion ground state, a flat plateau at 1/3)\n\nThe local filling is a flat plateau at nu = 13 across the sphere — the hallmark of the incompressible liquid. From here, Tutorial 1 explains every step in detail and adds the pair correlation; later tutorials cover excitations, higher fillings, fast unprojected sampling, partons, and energies.","title":"Quickstart"},{"category":"section","location":"#What's-inside","page":"Home","text":"Wavefunction Description Fast rank-1 updates?\nΨproj Jain–Kamilla projected CF state No (a move changes every column)\nΨparton Jain–Kamilla projected parton state No\nΨunproj unprojected det·Jastrow (single-particle orbitals) Yes (Sherman–Morrison)\nΨoneLL bare Jastrow (Laughlin) n/a","title":"What's inside"},{"category":"section","location":"#Where-to-go-next","page":"Home","text":"Physics background — LLL, composite fermions, the Jain–Kamilla projection, and the meaning of Qstar, p, and l_m_list.\nTutorials — hands-on, example-driven walkthroughs.\nAPI reference — every exported function and type.\nArchitecture — how the code is organized (for contributors).\nTheory & citation — the method, the derivation, and how to cite.","title":"Where to go next"}]
}
